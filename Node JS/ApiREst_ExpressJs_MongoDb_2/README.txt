> LIBRERÍAS UTILIZADAS
  "dependencies": {
    ("bcrypt": "^5.1.0" = no funcionó, en su lugar usé "bcryptjs")
    "bcryptjs": "^2.4.3" = para encriptar la contraseña del usuario
    "express": "^4.17.1" = administra el manejo de rutas (carpeta routes)
    "jsonwebtoken": "^9.0.0" = para manejar los token de usuario
    "moment": "^2.29.4" = para el manejo de fechas
    "mongoose": "^5.13.3" = para conectarnos a la BD de MongoDb en Atlas
    "connect-multiparty": "^2.2.0" = para el manejo de ficheros (imágenes o archvos)
  }